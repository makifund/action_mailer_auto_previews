require 'spec_helper'

describe ActionMailerAutoPreviews do
  it 'has a version number' do
    expect(ActionMailerAutoPreviews::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
